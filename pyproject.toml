[project]
name = "mcp-sdk-usage-kusto"
version = "0.1.0"
description = "MCP server for Azure SDK usage data queries via Kusto (stdio mode)"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.14.1",
    "azure-identity>=1.25.0",
    "requests>=2.32.0"
]