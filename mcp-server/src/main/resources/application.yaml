spring:
  application:
    name: bi-mcp-server

  main:
    banner-mode: off
    web-application-type: none

  ai:
    mcp:
      server:
        name: bi-mcp-server
        version: 1.0.0
        type: SYNC
        stdio: true

logging:
  pattern:
    console: ""